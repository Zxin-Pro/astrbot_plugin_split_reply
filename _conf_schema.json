{
  "streaming_strategy": {
    "description": "流式策略：force_non_streaming=强制本条消息走非流式，完整返回后分段（推荐，稳定）；streaming_split=保持流式打字机效果，按换行实时分段发送",
    "type": "string",
    "hint": "force_non_streaming 或 streaming_split",
    "default": "force_non_streaming"
  },
  "split_mode": {
    "description": "拆分模式：newline=按单个换行符拆分；blank_line=按空行拆分（保留段内换行）",
    "type": "string",
    "hint": "newline 或 blank_line",
    "default": "newline"
  },
  "delay_seconds": {
    "description": "每条分段消息之间的发送延迟（秒），防止触发平台风控",
    "type": "float",
    "hint": "建议 0.3 ~ 1.5",
    "default": 0.5
  },
  "max_length": {
    "description": "单条分段消息最大长度（字符数），超过则截断；0 表示不限制",
    "type": "int",
    "hint": "0 = 不限制",
    "default": 0
  }
}
